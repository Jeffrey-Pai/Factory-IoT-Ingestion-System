# Optional absolute base URL for the backend API.
# Leave empty (default) to use same-origin relative paths — which is what the Vite
# dev proxy and the nginx production image both expect. Set this only when the
# dashboard is served from a different origin than the API.
VITE_API_BASE=
